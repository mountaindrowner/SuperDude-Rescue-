# Local Claude Code workspace (per-machine permissions etc.).
.claude/

# OS / editor scratch files.
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Node modules in case a dev installs locally for the tools/ scripts.
# (package-lock.json IS tracked - Codemagic's `npm ci` requires it.)
node_modules/

# Capacitor / iOS wrap (generated on the build Mac).
www/
ios/
android/
.gradle/
*.xcuserstate

# Element Lab dev-test output (screenshots regenerate on every test run)
games/element-lab/test/shot-*.png
